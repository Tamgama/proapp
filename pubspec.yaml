name: proapp
description: A new Flutter practice.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  file_picker: ^5.2.3

  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.10+1

  english_words: ^4.0.0
  provider: ^6.0.0

  animate_do: ^3.3.4

  mongo_dart: ^0.9.0

 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
      - assets/
